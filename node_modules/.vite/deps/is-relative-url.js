import "./chunk-BUSYA2B4.js";

// node_modules/is-absolute-url/index.js
var ABSOLUTE_URL_REGEX = /^[a-zA-Z][a-zA-Z\d+\-.]*?:/;
var WINDOWS_PATH_REGEX = /^[a-zA-Z]:\\/;
function isAbsoluteUrl(url) {
  if (typeof url !== "string") {
    throw new TypeError(`Expected a \`string\`, got \`${typeof url}\``);
  }
  if (WINDOWS_PATH_REGEX.test(url)) {
    return false;
  }
  return ABSOLUTE_URL_REGEX.test(url);
}

// node_modules/is-relative-url/index.js
function isRelativeUrl(url, options = {}) {
  const { allowProtocolRelative = true } = options;
  if (!allowProtocolRelative && typeof url === "string" && url.startsWith("//")) {
    return false;
  }
  return !isAbsoluteUrl(url);
}
export {
  isRelativeUrl as default
};
//# sourceMappingURL=is-relative-url.js.map
